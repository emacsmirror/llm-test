* 0.2.3
- Add =run-command= to avoid issues in using =M-x= directly.
- Fix to properly handling reserved key names on input.
- Fix for long outputs from emacsclient.
* 0.2.2
- Fixes to =type-text= to input the text more realistically.
* 0.2.1
- Add ~type-text~ tool for reliable text entry.
- Support enhanced debug mode with ~llm-test-debug~ or the ~LLM_TEST_DEBUG~ environment variable.
- Suppress native compilation noise and filter out the ~*Warnings*~ buffer from the test agent's view.
- Support tagging tests as expected failures in YAML.
- Tweak instructions to stop the test from trying to mock things that aren't working.
* 0.2.0
- Added =require= for all common providers so users don't need to specifically require them.
- Fix bug in registering ert tests in use of ~:extra-load-path~.
- Suppress non-free warnings in tests.
- Fix failure to fail with some llm providers.
- Add ability to test for expected failures (mostly for testing =llm-test= itself).
